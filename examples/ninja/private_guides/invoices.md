# Private Invoice Operations

The private billing service owns invoice data and returns validated models for
reading, creating, updating, and deleting invoices. Public callers must pass
through the public API's authentication and ACL checks first.
